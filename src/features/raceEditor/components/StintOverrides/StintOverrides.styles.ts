import styled from '@emotion/styled';

export const SplitItem = styled.div`
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 4px 0;
`;

export const IgnoredText = styled.span`
    text-decoration: line-through;
    opacity: 0.5;
`;

export const ManualBadge = styled.span`
    color: #ff9800;
    font-size: 0.8em;
    font-weight: bold;
`;
