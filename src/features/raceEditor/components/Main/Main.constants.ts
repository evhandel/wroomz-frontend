import type { Team } from '../Teams/Teams.types';
import type { StintsByPilotsData } from './Main.types';

export const defaultTeams: Team[] = [];

export const defaultStintsByPilots: StintsByPilotsData = {};
